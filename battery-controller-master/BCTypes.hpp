#ifndef BC_TYPES_HPP
#define BC_TYPES_HPP

#include <mbed.h>

typedef int32_t current_t; // Current in mA - positive for charge, negative for discharge
typedef int32_t voltage_t; // Voltages in mV

typedef uint16_t temperature_t; // Temperatures in 1/10 degree Celsius

#endif
